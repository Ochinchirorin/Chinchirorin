const state = {
  currentPlayer: 0,
};

const mutations = {
  SET_CURRENT_PLAYER: (state, index) => (state.currentPlayer = index),
};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
